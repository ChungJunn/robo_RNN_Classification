TODO

1) no using init() for hidden (RNN module takes care of it itself?)
2) no use for loop when using rnn (do it in one que)


3) [checked] use name rnn for the model

4) make sure train,valid,test are shuffled before split
5) make recipe - shell script
6) document directory, results, so that you will remember 
